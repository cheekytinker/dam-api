import { describe, it } from 'mocha';

describe('unit', () => {
  describe('damProviderController', () => {
    describe('when reading a dam provider successfully', () => {
      it('should return 200');
    });
  });
});
